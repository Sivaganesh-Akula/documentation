# Removing files

To stage the deleted files

command:

- `git rm`

suffixs

- f -> force removal if file is modified in stage and deleted on mofified or untracked
- --cached -> to delete the file in stage area
- we can pass files, directioried and file-glob patterns (log/\*.log)
